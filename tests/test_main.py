# -*- coding: utf-8 -*-


# TODO: Before these tests can easily be created, I need to abstract everything
#       that is related to generating the tweet content to a separate function.
#       Once that is done, I only need to mock that one functions return value.

def test_sending_creates_history_entry():
    """"Test sending in normal mode creates a history entry."""
    pass

# TODO: Test sending duplicate in normal mode leads to error to user.
# TODO: Test sending in normal mode shows a console message.
# TODO: Test sending in normal mode prints success msg to console
